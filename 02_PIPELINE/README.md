# Pipeline

This folder is a portfolio locator, not a workspace.

- `TOPIC_BACKLOG.csv` stores unassigned candidates and qualification fields.
- `VIDEO_INDEX.csv` lists only video packages that actually exist.

Once durable information belongs to a specific video ID, it belongs inside that video's folder.
