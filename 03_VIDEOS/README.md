# Video Packages

Each `VIDEO_0XX_SLUG` is a self-contained production record and retains its ID permanently.

Use `_VIDEO_TEMPLATE` through `tools/new_video.py` once a topic is approved for a durable package.

Do not create duplicate numeric IDs. Do not store one video's research, scripts, sources, assets or analytics in another video folder.
